# Copy to .env and replace both values before exposing the instance.
# docker compose up will refuse to start if TURN_AUTH_SECRET is unset.
TURN_AUTH_SECRET=replace-with-a-long-random-string
# Required for a public deployment. Must match the site origin, e.g. https://example.com
ALLOWED_ORIGIN=
