# Copy to .env and replace the secret before exposing the instance.
TURN_AUTH_SECRET=replace-with-a-long-random-string
# Optional. When set, WebSocket connections must present this Origin.
ALLOWED_ORIGIN=
